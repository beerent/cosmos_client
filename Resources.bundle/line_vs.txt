#version 100
precision highp float;
attribute vec3 coord3d;
attribute vec3 vColor;
uniform mat4 mMVP;
varying vec3 fColor;                   
void main(void) {
gl_Position = mMVP * vec4(coord3d, 1.0);
fColor = vColor;
}